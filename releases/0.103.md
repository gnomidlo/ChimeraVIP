# ChimeraVIP 0.103

Cleanup przed 1.0.

- `help`, `containers`, `combat_colors`, `characters` i `stats` korzystaja szerzej ze wspolnego `core/util.lua`;
- `util.lua` dostal wspolne `deep_copy`, `merge_defaults` i `pcre_escape`;
- Quiet Footer pomija renderowanie vitals, jesli dana wartosc GMCP nie zmienila sie od poprzedniego eventu;
- usunieto lokalne kasowanie starego `progression.lua`; sprzatanie plikow nalezy do `manifest.remove`;
- potwierdzona zgodnosc z upstream Chimery 4.3;
- odswiezono README i dokumentacje aktualnej architektury.

Brak nowych komend i zmian w zachowaniu parserow.
