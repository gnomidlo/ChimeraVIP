# ChimeraVIP 0.97

Hotfix formatowania ekwipunku.

- poprawiono wywolanie API Mudleta: `creplace` zamiast nieistniejacego `cReplace`;
- zachowano lekkie formatowanie linii `RECE`, `EKWIPUNEK` i `PRZY SOBIE` bez gagowania calych odpowiedzi;
- brak nowych komend i zmian w centralnej pomocy.
